Just a project to code something with [Haxe](http://haxe.org/).
Based on my implementation in [Go](https://github.com/gotnospirit/messageformat).

Run Unit Test:

`cls && haxe -main Main --interp -cp src -cp tests`
